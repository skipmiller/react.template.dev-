# Application
NODE_ENV=development
PORT=3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentication (for future setup)
# AUTH_SECRET=your_auth_secret  # Generate with: openssl rand -base64 32
# NEXTAUTH_URL=http://localhost:3000

# Database (for future setup)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/your_db_name
# DATABASE_DIRECT_URL=postgresql://postgres:password@localhost:5432/your_db_name
# Database connection pool configuration
# DATABASE_POOL_SIZE=10

# External Services (examples for future use)
# NEXT_PUBLIC_GA_TRACKING_ID=your_ga_tracking_id

# Email Service (for future setup)
# EMAIL_SERVER_HOST=smtp.example.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your_username
# EMAIL_SERVER_PASSWORD=your_password
# EMAIL_FROM=noreply@example.com

# Payment Processing (for future setup)
# STRIPE_API_KEY=your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

# API Keys (for future setup)
# OPENAI_API_KEY=your_openai_api_key

# Feature Flags
# NEXT_PUBLIC_FEATURE_AUTH_ENABLED=true
# NEXT_PUBLIC_FEATURE_PAYMENTS_ENABLED=false

# API Keys (replace with your actual keys in .env file)
ANTHROPIC_API_KEY=your_api_key_here

# Development Configuration
NODE_ENV=development
PORT=3000

# Feature Flags
ENABLE_MCP_SERVERS=true
ENABLE_TEXT_EDITOR_TOOL=true
ENABLE_TOKEN_EFFICIENCY=true 